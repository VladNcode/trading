Starting up:

```bash
docker compose up -d
```

Shutting down:

```bash
docker compose down
```

Run tests:

```bash
npm run test
```

Navigate to <a href="http://localhost:3000/static" target="_blank">http://localhost:3000/static</a>
